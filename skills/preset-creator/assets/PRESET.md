---
schema: preset-document/v1
description: Replace this with one sentence that explains what the preset enables.
whenToUse: Replace this with the conditions that make this preset the right explicit choice.
inputs:
  example: Explain one declared input, or remove the inputs block.
entrypoints:
  plan: ha preset entrypoint <preset-id> plan --task <task-id> --allow-scripts
---

# Preset Title

Explain the workflow, constraints, and examples that a person or agent needs after choosing this preset.
