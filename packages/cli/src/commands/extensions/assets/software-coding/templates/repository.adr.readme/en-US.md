# ADR

## Usage

- Store lightweight architecture decision records in this directory only when a repository-local ADR is more appropriate than a harness decision package.
- For load-bearing harness choices, prefer `ha decision propose ...` so the decision gets lifecycle state, fact evidence, and relation edges.
- Keep accepted ADRs linked from the relevant task or decision entity.
