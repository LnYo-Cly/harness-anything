# Progress

进展必须通过 `ha task progress append ... --evidence type:PATH:summary` 记录；不得只保留模板内容。progress 是时间线，不是 fact。承重观察必须另用 `ha record fact --task <task-id> ...` 写入 `facts.md`。

## Log

- 记录关键实现步骤、验证结果和阻塞。

## Evidence

| ID | Type | Evidence | Status |
| --- | --- | --- | --- |
