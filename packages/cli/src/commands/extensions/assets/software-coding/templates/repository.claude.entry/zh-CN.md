# Claude Harness Entry

Follow `AGENTS.md` and the shared authored harness under `harness/`. Treat this as a compatibility pointer, not a second rule source. The required workflow is triadic: task progress for work state, task-local facts for load-bearing observations, decisions for load-bearing choices, and relations for cross-entity links.
