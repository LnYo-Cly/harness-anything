# Contracts Moved

The active contracts have been migrated to `harness/contracts/`.

This legacy path is retained only as a historical pointer. New references should use the project-root-relative `harness/contracts/` paths.
